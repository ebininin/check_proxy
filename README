Check provided IPv4(s) in 'proxy_list' through 'icanhazip.com' and return proxies IP or failured

proxy_list format - 1 IPv4 each line:
- For single port:  ipv4:port:user:password
- For multi port:   ipv4:port-range:user:password
